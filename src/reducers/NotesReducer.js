const initalState = {
    list: [
        { title: 'Primeira Nota', body: 'Testando' }
    ]
};



export default (state = initalState, action) => {

    switch(action.type) {
        case 'ADD_NOTE':

        break;
    }

    return state;
}